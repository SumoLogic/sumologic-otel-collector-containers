# Add 'documentation' label to any change to *.md files
documentation:
  - any: ["**/*.md"]

# Add 'docker' label to any change in docker related files
docker:
  - any: ["**/Dockerfile*", "**/.dockerignore", "**/*bake*.hcl"]

# Add 'github_actions' label to any change .github/ directory
github_actions:
  - any: [".github/**"]
